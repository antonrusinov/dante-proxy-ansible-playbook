- hosts: digitalocean
  user: root
  vars:
    port:      8888
    username:  default
    password:  very_secret_password
    hash_solt: very_secret_solt

  roles:
    - {name: isntall-dante, tags: [isntall, run]}
    - {name: stop-dante, tags: [stop]}
    - {name: configure-dante, tags: [config, run, restart]}
    - {name: start-dante, tags: [start, run, restart]}
